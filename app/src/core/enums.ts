export enum ClusterID {
  MainNet = 'mainnet-beta',
  TestNet = 'testnet',
  DevNet = 'devnet',
  Custom = 'custom',
  LocalNet = 'localhost'
}

export enum WalletProviders {
  Sollet = 'Sollect',
  Phantom = 'Phantom'
}
