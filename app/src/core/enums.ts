export enum ClusterID {
  MainNet = 'mainnet',
  TestNet = 'testnet',
  DevNet = 'devnet',
  Custom = 'custom',
  LocalNet = 'localhost'
}

export enum WalletProviders {
  Sollet = 0,
  Phantom = 1
}
